OBJECTIVE
- Your task is to 1) replace placeholders based on instructions, and 2) Adjust some input prompts to fit custom instructions in a natural way

INSTRUCTIONS:
1. Replace "[PLAYERS]" placeholder with one of the players: {{players_array:inline}}.
- Ensure that names are evenly distributed based on HISTORY.
- Ensure that names are contextually chosen based on task type, question type, and custom instructions.
- Select male or female names based on prompt context
- Only replace "[PLAYERS]" placeholder, do not add names without placeholder.

2. Replace "[DRINKS]" placeholder with x sips: {{drinking_level}}
- Use DRINKING VARIATION.
    <<DRINKING VARIATION (Choose one that fits grammatically, or modify to fit)
    - ta x slurk(er)
    - tar x slurker
    - drikke x
    - drikker x
    - ta x shot(s)
    - må shotte
    - må chugge
    - shot
    - chug>>
- Follow Norwegian grammar rules.
- Use a mix of written numbers and digits.
- Only replace "[DRINKS]" placeholder, do not add drinks without placeholder (if not mentioned in custom instructions).

3. Adjust relevant prompts based on the custom instructionss, but don't force it if irrelevant.

CUSTOM INSTRUCTIONS:
"{{custom_instructions}}"

OUTPUT FORMAT
- Return the prompts based on instructions.
- Never translate any of the INPUT PROMPTS: If a word is English, keep it English. If it's Norwegian, keep it Norwegian.
- Return one line per input prompt.
- No numbering or newlines.

===

INPUT PROMPTS:
{{input_to_modify:list}}
END OF INPUT PROMPTS

===

##HISTORY:
{{generation_history:list}}